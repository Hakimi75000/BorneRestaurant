<Window x:Class="BorneRestaurant.views.ModifierCategorie"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modifier catégorie"
        Height="300"
        Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="25">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Nom label -->
            <RowDefinition Height="Auto"/>
            <!-- Nom textbox -->
            <RowDefinition Height="Auto"/>
            <!-- Description label -->
            <RowDefinition Height="*"/>
            <!-- Description textbox -->
            <RowDefinition Height="Auto"/>
            <!-- Bouton -->
        </Grid.RowDefinitions>

        <!-- NOM -->
        <TextBlock Text="Nom"
                   FontWeight="Bold"
                   Margin="0,0,0,5"
                   Grid.Row="0"/>

        <TextBox x:Name="TxtNom"
                 Height="30"
                 Grid.Row="1"
                 Margin="0,0,0,15"/>

        <!-- DESCRIPTION -->
        <TextBlock Text="Description"
                   FontWeight="Bold"
                   Margin="0,0,0,5"
                   Grid.Row="2"/>

        <TextBox x:Name="TxtDescription"
                 Grid.Row="3"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- BOUTON -->
        <Button Content="Enregistrer"
                Width="150"
                Height="40"
                Background="DarkRed"
                Foreground="White"
                FontSize="16"
                HorizontalAlignment="Right"
                Margin="0,15,0,0"
                Grid.Row="4"
                Click="BtnEnregistrer_Click"/>

    </Grid>
</Window>
